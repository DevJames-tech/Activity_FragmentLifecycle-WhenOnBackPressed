package com.example.fragmentactivitylifecycle;

public class AppSummary {

    /**  What happens to both the fragment and activity Lifecycle when the back button is pressed
     *
     * if there is no fragment on the backstack
     *
     * On creation of the activity onCreate, onStart and onResume is called for Fragment
     * on BackPress when unhandled onPause, onStop, and on Destroy is called
     *
     * -- The fragment and activity is both destroyed...
     * **/
}
